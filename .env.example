# AI Diagram 기능(선택 텍스트 → Mermaid 변환)에 사용할 OpenAI API 키.
# 아래 내용을 복사해 같은 폴더에 ".env" 파일로 저장하고 실제 키를 채워 넣으세요.
# .env 파일은 .gitignore에 포함되어 있어 git에는 커밋되지 않습니다.

OPENAI_API_KEY=sk-...

# 선택: 사용할 모델을 바꾸고 싶을 때만 설정 (기본값: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
